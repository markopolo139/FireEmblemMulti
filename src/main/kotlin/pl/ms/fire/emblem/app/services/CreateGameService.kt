package pl.ms.fire.emblem.app.services

import org.springframework.stereotype.Service

@Service
class CreateGameService {

    //TODO: create token, join game (for both validate if user is in another game, for second validate both players), exit game(delete board)
}