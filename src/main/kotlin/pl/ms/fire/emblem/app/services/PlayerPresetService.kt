package pl.ms.fire.emblem.app.services

import org.springframework.stereotype.Service

@Service
class PlayerPresetService {

    //TODO: create preset(list)(set remaining hp to hp + class hp), delete preset(list), select preset,
    // return presets, validate (if sum of stats is equal 25, if in every preset ar 5 characters) (for delete and select validate if id of preset exists)

}